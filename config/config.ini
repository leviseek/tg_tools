[Settings]
; 优先级选项: idle, below, normal, above, high, realtime
SGuardSvc64_priority = idle
SGuard64_priority = idle

; 核心选择: last4, first4, 或十六进制掩码 (如 0xF0)
affinity_mask = last4

; 是否启用效能模式: 1=启用, 0=禁用
enable_eco = 1